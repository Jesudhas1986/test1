BUILDBOT_URL = "http://<buildbot-host>:<port>/api/v2/workers"

def get_workers():
    resp = requests.get(BUILDBOT_URL)
    resp.raise_for_status()  # raise error if API call fails
    data = resp.json()

    workers = data.get("workers", [])
    for w in workers:
        worker_id = w["workerid"]
        name = w["name"]
        print(f"ID: {worker_id} | Name: {name}")

if __name__ == "__main__":
    get_workers()
4. Example Output
yaml
Copy
Edit
ID: 1 | Name: linux-worker-1
ID: 2 | Name: windows-worker-1
ID: 3 | Name: macos-worker-1
Do you also want me to show you how to map a build → worker name (since each build also stores its workerid, just like builderid)?









Unlock more with Plus
