1. If you’re using --user and --password

Make sure you don’t pass the API key as password unless intended. Some Artifactory setups expect headers instead.

wget --user=<USERNAME> --password=<PASSWORD> "https://<ARTIFACTORY_URL>/artifactory/<REPO>/<PATH>/<FILE>"


If your password or API key contains special characters (@, :, !, etc.), wrap it in quotes or use the header method.

2. If you’re using an API Key

You can’t use --password with just the API key (that’s what triggers “unknown authentication scheme”).
Instead, add it as a header:

wget --header="X-JFrog-Art-Api: <API_KEY>" "https://<ARTIFACTORY_URL>/artifactory/<REPO>/<PATH>/<FILE>"

3. If you’re using a Bearer Token

Use the proper Authorization header:

wget --header="Authorization: Bearer <ACCESS_TOKEN>" "https://<ARTIFACTORY_URL>/artifactory/<REPO>/<PATH>/<FILE>"

4. Debugging

If you still get errors, run:

wget --debug "https://<ARTIFACTORY_URL>/artifactory/<REPO>/<PATH>/<FILE>"


That will show exactly what authentication scheme wget is attempting.

⚡ Quick rule:

Username + Password → --user + --password

API Key → --header="X-JFrog-Art-Api: <API_KEY>"

Bearer Token → --header="Authorization: Bearer <TOKEN>"
